# ABI Specifications

Contains executable, weight-shard, runtime/driver ioctl, command-queue, completion, and telemetry
contracts. ABI changes update compiler, runtime, driver, firmware, and tests together.
