`timescale 1ns/1ps // Define simulation time units for generated KD28 cells.
`default_nettype none // Reject accidental implicit nets in generated KD28 cells.

// Generated by scripts/generate_kd28_sram_library.py; compile this file for functional simulation only.
// Source inputs are Library/models/kd28/sram/macros.yaml and Library/timing/kd28/sram/profiles.yaml.

module KD28_SRAM_SP_256X32 ( // Define the fixed SP KD28 SRAM cell.
    input wire CLK, // Receive the synchronous SRAM clock.
    input wire CS, // Enable one SRAM operation.
    input wire WE, // Select write when high and read when low.
    input wire [7:0] A, // Select the SRAM word address.
    input wire [31:0] D, // Receive write data.
    input wire [3:0] WM, // Enable active-high byte writes.
    output wire [31:0] Q // Return the registered read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_sp_model #( // Bind the fixed cell to the portable SP behavior.
        .DATA_WIDTH(32), // Fix the generated data width.
        .DEPTH(256), // Fix the generated word depth.
        .ADDR_WIDTH(8), // Fix the generated address width.
        .MASK_WIDTH(4) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .clk_i(CLK), // Connect clk_i to the fixed-cell CLK pin.
        .cs_i(CS), // Connect cs_i to the fixed-cell CS pin.
        .we_i(WE), // Connect we_i to the fixed-cell WE pin.
        .addr_i(A), // Connect addr_i to the fixed-cell A pin.
        .wdata_i(D), // Connect wdata_i to the fixed-cell D pin.
        .wmask_i(WM), // Connect wmask_i to the fixed-cell WM pin.
        .rdata_o(Q) // Connect rdata_o to the fixed-cell Q pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_SP_256X32 functional cell.

module KD28_SRAM_SP_512X64 ( // Define the fixed SP KD28 SRAM cell.
    input wire CLK, // Receive the synchronous SRAM clock.
    input wire CS, // Enable one SRAM operation.
    input wire WE, // Select write when high and read when low.
    input wire [8:0] A, // Select the SRAM word address.
    input wire [63:0] D, // Receive write data.
    input wire [7:0] WM, // Enable active-high byte writes.
    output wire [63:0] Q // Return the registered read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_sp_model #( // Bind the fixed cell to the portable SP behavior.
        .DATA_WIDTH(64), // Fix the generated data width.
        .DEPTH(512), // Fix the generated word depth.
        .ADDR_WIDTH(9), // Fix the generated address width.
        .MASK_WIDTH(8) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .clk_i(CLK), // Connect clk_i to the fixed-cell CLK pin.
        .cs_i(CS), // Connect cs_i to the fixed-cell CS pin.
        .we_i(WE), // Connect we_i to the fixed-cell WE pin.
        .addr_i(A), // Connect addr_i to the fixed-cell A pin.
        .wdata_i(D), // Connect wdata_i to the fixed-cell D pin.
        .wmask_i(WM), // Connect wmask_i to the fixed-cell WM pin.
        .rdata_o(Q) // Connect rdata_o to the fixed-cell Q pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_SP_512X64 functional cell.

module KD28_SRAM_SP_1024X128 ( // Define the fixed SP KD28 SRAM cell.
    input wire CLK, // Receive the synchronous SRAM clock.
    input wire CS, // Enable one SRAM operation.
    input wire WE, // Select write when high and read when low.
    input wire [9:0] A, // Select the SRAM word address.
    input wire [127:0] D, // Receive write data.
    input wire [15:0] WM, // Enable active-high byte writes.
    output wire [127:0] Q // Return the registered read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_sp_model #( // Bind the fixed cell to the portable SP behavior.
        .DATA_WIDTH(128), // Fix the generated data width.
        .DEPTH(1024), // Fix the generated word depth.
        .ADDR_WIDTH(10), // Fix the generated address width.
        .MASK_WIDTH(16) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .clk_i(CLK), // Connect clk_i to the fixed-cell CLK pin.
        .cs_i(CS), // Connect cs_i to the fixed-cell CS pin.
        .we_i(WE), // Connect we_i to the fixed-cell WE pin.
        .addr_i(A), // Connect addr_i to the fixed-cell A pin.
        .wdata_i(D), // Connect wdata_i to the fixed-cell D pin.
        .wmask_i(WM), // Connect wmask_i to the fixed-cell WM pin.
        .rdata_o(Q) // Connect rdata_o to the fixed-cell Q pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_SP_1024X128 functional cell.

module KD28_SRAM_SP_2048X256 ( // Define the fixed SP KD28 SRAM cell.
    input wire CLK, // Receive the synchronous SRAM clock.
    input wire CS, // Enable one SRAM operation.
    input wire WE, // Select write when high and read when low.
    input wire [10:0] A, // Select the SRAM word address.
    input wire [255:0] D, // Receive write data.
    input wire [31:0] WM, // Enable active-high byte writes.
    output wire [255:0] Q // Return the registered read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_sp_model #( // Bind the fixed cell to the portable SP behavior.
        .DATA_WIDTH(256), // Fix the generated data width.
        .DEPTH(2048), // Fix the generated word depth.
        .ADDR_WIDTH(11), // Fix the generated address width.
        .MASK_WIDTH(32) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .clk_i(CLK), // Connect clk_i to the fixed-cell CLK pin.
        .cs_i(CS), // Connect cs_i to the fixed-cell CS pin.
        .we_i(WE), // Connect we_i to the fixed-cell WE pin.
        .addr_i(A), // Connect addr_i to the fixed-cell A pin.
        .wdata_i(D), // Connect wdata_i to the fixed-cell D pin.
        .wmask_i(WM), // Connect wmask_i to the fixed-cell WM pin.
        .rdata_o(Q) // Connect rdata_o to the fixed-cell Q pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_SP_2048X256 functional cell.

module KD28_SRAM_SDP_256X32 ( // Define the fixed SDP KD28 SRAM cell.
    input wire WCLK, // Receive the synchronous write clock.
    input wire WCS, // Enable one write operation.
    input wire [7:0] WA, // Select the write word address.
    input wire [31:0] D, // Receive write data.
    input wire [3:0] WM, // Enable active-high byte writes.
    input wire RCLK, // Receive the synchronous read clock.
    input wire RCS, // Enable one registered read operation.
    input wire [7:0] RA, // Select the read word address.
    output wire [31:0] Q // Return the registered read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_sdp_model #( // Bind the fixed cell to the portable SDP behavior.
        .DATA_WIDTH(32), // Fix the generated data width.
        .DEPTH(256), // Fix the generated word depth.
        .ADDR_WIDTH(8), // Fix the generated address width.
        .MASK_WIDTH(4) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .write_clk_i(WCLK), // Connect write_clk_i to the fixed-cell WCLK pin.
        .write_cs_i(WCS), // Connect write_cs_i to the fixed-cell WCS pin.
        .write_addr_i(WA), // Connect write_addr_i to the fixed-cell WA pin.
        .write_data_i(D), // Connect write_data_i to the fixed-cell D pin.
        .write_mask_i(WM), // Connect write_mask_i to the fixed-cell WM pin.
        .read_clk_i(RCLK), // Connect read_clk_i to the fixed-cell RCLK pin.
        .read_cs_i(RCS), // Connect read_cs_i to the fixed-cell RCS pin.
        .read_addr_i(RA), // Connect read_addr_i to the fixed-cell RA pin.
        .read_data_o(Q) // Connect read_data_o to the fixed-cell Q pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_SDP_256X32 functional cell.

module KD28_SRAM_SDP_512X64 ( // Define the fixed SDP KD28 SRAM cell.
    input wire WCLK, // Receive the synchronous write clock.
    input wire WCS, // Enable one write operation.
    input wire [8:0] WA, // Select the write word address.
    input wire [63:0] D, // Receive write data.
    input wire [7:0] WM, // Enable active-high byte writes.
    input wire RCLK, // Receive the synchronous read clock.
    input wire RCS, // Enable one registered read operation.
    input wire [8:0] RA, // Select the read word address.
    output wire [63:0] Q // Return the registered read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_sdp_model #( // Bind the fixed cell to the portable SDP behavior.
        .DATA_WIDTH(64), // Fix the generated data width.
        .DEPTH(512), // Fix the generated word depth.
        .ADDR_WIDTH(9), // Fix the generated address width.
        .MASK_WIDTH(8) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .write_clk_i(WCLK), // Connect write_clk_i to the fixed-cell WCLK pin.
        .write_cs_i(WCS), // Connect write_cs_i to the fixed-cell WCS pin.
        .write_addr_i(WA), // Connect write_addr_i to the fixed-cell WA pin.
        .write_data_i(D), // Connect write_data_i to the fixed-cell D pin.
        .write_mask_i(WM), // Connect write_mask_i to the fixed-cell WM pin.
        .read_clk_i(RCLK), // Connect read_clk_i to the fixed-cell RCLK pin.
        .read_cs_i(RCS), // Connect read_cs_i to the fixed-cell RCS pin.
        .read_addr_i(RA), // Connect read_addr_i to the fixed-cell RA pin.
        .read_data_o(Q) // Connect read_data_o to the fixed-cell Q pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_SDP_512X64 functional cell.

module KD28_SRAM_SDP_1024X128 ( // Define the fixed SDP KD28 SRAM cell.
    input wire WCLK, // Receive the synchronous write clock.
    input wire WCS, // Enable one write operation.
    input wire [9:0] WA, // Select the write word address.
    input wire [127:0] D, // Receive write data.
    input wire [15:0] WM, // Enable active-high byte writes.
    input wire RCLK, // Receive the synchronous read clock.
    input wire RCS, // Enable one registered read operation.
    input wire [9:0] RA, // Select the read word address.
    output wire [127:0] Q // Return the registered read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_sdp_model #( // Bind the fixed cell to the portable SDP behavior.
        .DATA_WIDTH(128), // Fix the generated data width.
        .DEPTH(1024), // Fix the generated word depth.
        .ADDR_WIDTH(10), // Fix the generated address width.
        .MASK_WIDTH(16) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .write_clk_i(WCLK), // Connect write_clk_i to the fixed-cell WCLK pin.
        .write_cs_i(WCS), // Connect write_cs_i to the fixed-cell WCS pin.
        .write_addr_i(WA), // Connect write_addr_i to the fixed-cell WA pin.
        .write_data_i(D), // Connect write_data_i to the fixed-cell D pin.
        .write_mask_i(WM), // Connect write_mask_i to the fixed-cell WM pin.
        .read_clk_i(RCLK), // Connect read_clk_i to the fixed-cell RCLK pin.
        .read_cs_i(RCS), // Connect read_cs_i to the fixed-cell RCS pin.
        .read_addr_i(RA), // Connect read_addr_i to the fixed-cell RA pin.
        .read_data_o(Q) // Connect read_data_o to the fixed-cell Q pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_SDP_1024X128 functional cell.

module KD28_SRAM_SDP_2048X256 ( // Define the fixed SDP KD28 SRAM cell.
    input wire WCLK, // Receive the synchronous write clock.
    input wire WCS, // Enable one write operation.
    input wire [10:0] WA, // Select the write word address.
    input wire [255:0] D, // Receive write data.
    input wire [31:0] WM, // Enable active-high byte writes.
    input wire RCLK, // Receive the synchronous read clock.
    input wire RCS, // Enable one registered read operation.
    input wire [10:0] RA, // Select the read word address.
    output wire [255:0] Q // Return the registered read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_sdp_model #( // Bind the fixed cell to the portable SDP behavior.
        .DATA_WIDTH(256), // Fix the generated data width.
        .DEPTH(2048), // Fix the generated word depth.
        .ADDR_WIDTH(11), // Fix the generated address width.
        .MASK_WIDTH(32) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .write_clk_i(WCLK), // Connect write_clk_i to the fixed-cell WCLK pin.
        .write_cs_i(WCS), // Connect write_cs_i to the fixed-cell WCS pin.
        .write_addr_i(WA), // Connect write_addr_i to the fixed-cell WA pin.
        .write_data_i(D), // Connect write_data_i to the fixed-cell D pin.
        .write_mask_i(WM), // Connect write_mask_i to the fixed-cell WM pin.
        .read_clk_i(RCLK), // Connect read_clk_i to the fixed-cell RCLK pin.
        .read_cs_i(RCS), // Connect read_cs_i to the fixed-cell RCS pin.
        .read_addr_i(RA), // Connect read_addr_i to the fixed-cell RA pin.
        .read_data_o(Q) // Connect read_data_o to the fixed-cell Q pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_SDP_2048X256 functional cell.

module KD28_SRAM_TDP_32X32 ( // Define the fixed TDP KD28 SRAM cell.
    input wire CLK, // Receive the shared synchronous SRAM clock.
    input wire ACS, // Enable one port A operation.
    input wire AWE, // Select a port A write when high.
    input wire [4:0] AA, // Select the port A word address.
    input wire [31:0] AD, // Receive port A write data.
    input wire [3:0] AWM, // Enable active-high port A byte writes.
    output wire [31:0] AQ, // Return the registered port A read word.
    input wire BCS, // Enable one port B operation.
    input wire BWE, // Select a port B write when high.
    input wire [4:0] BA, // Select the port B word address.
    input wire [31:0] BD, // Receive port B write data.
    input wire [3:0] BWM, // Enable active-high port B byte writes.
    output wire [31:0] BQ // Return the registered port B read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_tdp_model #( // Bind the fixed cell to the portable TDP behavior.
        .DATA_WIDTH(32), // Fix the generated data width.
        .DEPTH(32), // Fix the generated word depth.
        .ADDR_WIDTH(5), // Fix the generated address width.
        .MASK_WIDTH(4) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .clk_i(CLK), // Connect clk_i to the fixed-cell CLK pin.
        .a_cs_i(ACS), // Connect a_cs_i to the fixed-cell ACS pin.
        .a_we_i(AWE), // Connect a_we_i to the fixed-cell AWE pin.
        .a_addr_i(AA), // Connect a_addr_i to the fixed-cell AA pin.
        .a_wdata_i(AD), // Connect a_wdata_i to the fixed-cell AD pin.
        .a_wmask_i(AWM), // Connect a_wmask_i to the fixed-cell AWM pin.
        .a_rdata_o(AQ), // Connect a_rdata_o to the fixed-cell AQ pin.
        .b_cs_i(BCS), // Connect b_cs_i to the fixed-cell BCS pin.
        .b_we_i(BWE), // Connect b_we_i to the fixed-cell BWE pin.
        .b_addr_i(BA), // Connect b_addr_i to the fixed-cell BA pin.
        .b_wdata_i(BD), // Connect b_wdata_i to the fixed-cell BD pin.
        .b_wmask_i(BWM), // Connect b_wmask_i to the fixed-cell BWM pin.
        .b_rdata_o(BQ) // Connect b_rdata_o to the fixed-cell BQ pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_TDP_32X32 functional cell.

module KD28_SRAM_TDP_32X64 ( // Define the fixed TDP KD28 SRAM cell.
    input wire CLK, // Receive the shared synchronous SRAM clock.
    input wire ACS, // Enable one port A operation.
    input wire AWE, // Select a port A write when high.
    input wire [4:0] AA, // Select the port A word address.
    input wire [63:0] AD, // Receive port A write data.
    input wire [7:0] AWM, // Enable active-high port A byte writes.
    output wire [63:0] AQ, // Return the registered port A read word.
    input wire BCS, // Enable one port B operation.
    input wire BWE, // Select a port B write when high.
    input wire [4:0] BA, // Select the port B word address.
    input wire [63:0] BD, // Receive port B write data.
    input wire [7:0] BWM, // Enable active-high port B byte writes.
    output wire [63:0] BQ // Return the registered port B read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_tdp_model #( // Bind the fixed cell to the portable TDP behavior.
        .DATA_WIDTH(64), // Fix the generated data width.
        .DEPTH(32), // Fix the generated word depth.
        .ADDR_WIDTH(5), // Fix the generated address width.
        .MASK_WIDTH(8) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .clk_i(CLK), // Connect clk_i to the fixed-cell CLK pin.
        .a_cs_i(ACS), // Connect a_cs_i to the fixed-cell ACS pin.
        .a_we_i(AWE), // Connect a_we_i to the fixed-cell AWE pin.
        .a_addr_i(AA), // Connect a_addr_i to the fixed-cell AA pin.
        .a_wdata_i(AD), // Connect a_wdata_i to the fixed-cell AD pin.
        .a_wmask_i(AWM), // Connect a_wmask_i to the fixed-cell AWM pin.
        .a_rdata_o(AQ), // Connect a_rdata_o to the fixed-cell AQ pin.
        .b_cs_i(BCS), // Connect b_cs_i to the fixed-cell BCS pin.
        .b_we_i(BWE), // Connect b_we_i to the fixed-cell BWE pin.
        .b_addr_i(BA), // Connect b_addr_i to the fixed-cell BA pin.
        .b_wdata_i(BD), // Connect b_wdata_i to the fixed-cell BD pin.
        .b_wmask_i(BWM), // Connect b_wmask_i to the fixed-cell BWM pin.
        .b_rdata_o(BQ) // Connect b_rdata_o to the fixed-cell BQ pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_TDP_32X64 functional cell.

module KD28_SRAM_TDP_32X128 ( // Define the fixed TDP KD28 SRAM cell.
    input wire CLK, // Receive the shared synchronous SRAM clock.
    input wire ACS, // Enable one port A operation.
    input wire AWE, // Select a port A write when high.
    input wire [4:0] AA, // Select the port A word address.
    input wire [127:0] AD, // Receive port A write data.
    input wire [15:0] AWM, // Enable active-high port A byte writes.
    output wire [127:0] AQ, // Return the registered port A read word.
    input wire BCS, // Enable one port B operation.
    input wire BWE, // Select a port B write when high.
    input wire [4:0] BA, // Select the port B word address.
    input wire [127:0] BD, // Receive port B write data.
    input wire [15:0] BWM, // Enable active-high port B byte writes.
    output wire [127:0] BQ // Return the registered port B read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_tdp_model #( // Bind the fixed cell to the portable TDP behavior.
        .DATA_WIDTH(128), // Fix the generated data width.
        .DEPTH(32), // Fix the generated word depth.
        .ADDR_WIDTH(5), // Fix the generated address width.
        .MASK_WIDTH(16) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .clk_i(CLK), // Connect clk_i to the fixed-cell CLK pin.
        .a_cs_i(ACS), // Connect a_cs_i to the fixed-cell ACS pin.
        .a_we_i(AWE), // Connect a_we_i to the fixed-cell AWE pin.
        .a_addr_i(AA), // Connect a_addr_i to the fixed-cell AA pin.
        .a_wdata_i(AD), // Connect a_wdata_i to the fixed-cell AD pin.
        .a_wmask_i(AWM), // Connect a_wmask_i to the fixed-cell AWM pin.
        .a_rdata_o(AQ), // Connect a_rdata_o to the fixed-cell AQ pin.
        .b_cs_i(BCS), // Connect b_cs_i to the fixed-cell BCS pin.
        .b_we_i(BWE), // Connect b_we_i to the fixed-cell BWE pin.
        .b_addr_i(BA), // Connect b_addr_i to the fixed-cell BA pin.
        .b_wdata_i(BD), // Connect b_wdata_i to the fixed-cell BD pin.
        .b_wmask_i(BWM), // Connect b_wmask_i to the fixed-cell BWM pin.
        .b_rdata_o(BQ) // Connect b_rdata_o to the fixed-cell BQ pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_TDP_32X128 functional cell.

module KD28_SRAM_TDP_256X32 ( // Define the fixed TDP KD28 SRAM cell.
    input wire CLK, // Receive the shared synchronous SRAM clock.
    input wire ACS, // Enable one port A operation.
    input wire AWE, // Select a port A write when high.
    input wire [7:0] AA, // Select the port A word address.
    input wire [31:0] AD, // Receive port A write data.
    input wire [3:0] AWM, // Enable active-high port A byte writes.
    output wire [31:0] AQ, // Return the registered port A read word.
    input wire BCS, // Enable one port B operation.
    input wire BWE, // Select a port B write when high.
    input wire [7:0] BA, // Select the port B word address.
    input wire [31:0] BD, // Receive port B write data.
    input wire [3:0] BWM, // Enable active-high port B byte writes.
    output wire [31:0] BQ // Return the registered port B read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_tdp_model #( // Bind the fixed cell to the portable TDP behavior.
        .DATA_WIDTH(32), // Fix the generated data width.
        .DEPTH(256), // Fix the generated word depth.
        .ADDR_WIDTH(8), // Fix the generated address width.
        .MASK_WIDTH(4) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .clk_i(CLK), // Connect clk_i to the fixed-cell CLK pin.
        .a_cs_i(ACS), // Connect a_cs_i to the fixed-cell ACS pin.
        .a_we_i(AWE), // Connect a_we_i to the fixed-cell AWE pin.
        .a_addr_i(AA), // Connect a_addr_i to the fixed-cell AA pin.
        .a_wdata_i(AD), // Connect a_wdata_i to the fixed-cell AD pin.
        .a_wmask_i(AWM), // Connect a_wmask_i to the fixed-cell AWM pin.
        .a_rdata_o(AQ), // Connect a_rdata_o to the fixed-cell AQ pin.
        .b_cs_i(BCS), // Connect b_cs_i to the fixed-cell BCS pin.
        .b_we_i(BWE), // Connect b_we_i to the fixed-cell BWE pin.
        .b_addr_i(BA), // Connect b_addr_i to the fixed-cell BA pin.
        .b_wdata_i(BD), // Connect b_wdata_i to the fixed-cell BD pin.
        .b_wmask_i(BWM), // Connect b_wmask_i to the fixed-cell BWM pin.
        .b_rdata_o(BQ) // Connect b_rdata_o to the fixed-cell BQ pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_TDP_256X32 functional cell.

module KD28_SRAM_TDP_512X64 ( // Define the fixed TDP KD28 SRAM cell.
    input wire CLK, // Receive the shared synchronous SRAM clock.
    input wire ACS, // Enable one port A operation.
    input wire AWE, // Select a port A write when high.
    input wire [8:0] AA, // Select the port A word address.
    input wire [63:0] AD, // Receive port A write data.
    input wire [7:0] AWM, // Enable active-high port A byte writes.
    output wire [63:0] AQ, // Return the registered port A read word.
    input wire BCS, // Enable one port B operation.
    input wire BWE, // Select a port B write when high.
    input wire [8:0] BA, // Select the port B word address.
    input wire [63:0] BD, // Receive port B write data.
    input wire [7:0] BWM, // Enable active-high port B byte writes.
    output wire [63:0] BQ // Return the registered port B read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_tdp_model #( // Bind the fixed cell to the portable TDP behavior.
        .DATA_WIDTH(64), // Fix the generated data width.
        .DEPTH(512), // Fix the generated word depth.
        .ADDR_WIDTH(9), // Fix the generated address width.
        .MASK_WIDTH(8) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .clk_i(CLK), // Connect clk_i to the fixed-cell CLK pin.
        .a_cs_i(ACS), // Connect a_cs_i to the fixed-cell ACS pin.
        .a_we_i(AWE), // Connect a_we_i to the fixed-cell AWE pin.
        .a_addr_i(AA), // Connect a_addr_i to the fixed-cell AA pin.
        .a_wdata_i(AD), // Connect a_wdata_i to the fixed-cell AD pin.
        .a_wmask_i(AWM), // Connect a_wmask_i to the fixed-cell AWM pin.
        .a_rdata_o(AQ), // Connect a_rdata_o to the fixed-cell AQ pin.
        .b_cs_i(BCS), // Connect b_cs_i to the fixed-cell BCS pin.
        .b_we_i(BWE), // Connect b_we_i to the fixed-cell BWE pin.
        .b_addr_i(BA), // Connect b_addr_i to the fixed-cell BA pin.
        .b_wdata_i(BD), // Connect b_wdata_i to the fixed-cell BD pin.
        .b_wmask_i(BWM), // Connect b_wmask_i to the fixed-cell BWM pin.
        .b_rdata_o(BQ) // Connect b_rdata_o to the fixed-cell BQ pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_TDP_512X64 functional cell.

module KD28_SRAM_TDP_1024X128 ( // Define the fixed TDP KD28 SRAM cell.
    input wire CLK, // Receive the shared synchronous SRAM clock.
    input wire ACS, // Enable one port A operation.
    input wire AWE, // Select a port A write when high.
    input wire [9:0] AA, // Select the port A word address.
    input wire [127:0] AD, // Receive port A write data.
    input wire [15:0] AWM, // Enable active-high port A byte writes.
    output wire [127:0] AQ, // Return the registered port A read word.
    input wire BCS, // Enable one port B operation.
    input wire BWE, // Select a port B write when high.
    input wire [9:0] BA, // Select the port B word address.
    input wire [127:0] BD, // Receive port B write data.
    input wire [15:0] BWM, // Enable active-high port B byte writes.
    output wire [127:0] BQ // Return the registered port B read word.
); // End the fixed KD28 SRAM cell interface.
    kd28_sram_tdp_model #( // Bind the fixed cell to the portable TDP behavior.
        .DATA_WIDTH(128), // Fix the generated data width.
        .DEPTH(1024), // Fix the generated word depth.
        .ADDR_WIDTH(10), // Fix the generated address width.
        .MASK_WIDTH(16) // Fix the generated byte-mask width.
    ) u_model ( // Instantiate the portable SRAM behavior.
        .clk_i(CLK), // Connect clk_i to the fixed-cell CLK pin.
        .a_cs_i(ACS), // Connect a_cs_i to the fixed-cell ACS pin.
        .a_we_i(AWE), // Connect a_we_i to the fixed-cell AWE pin.
        .a_addr_i(AA), // Connect a_addr_i to the fixed-cell AA pin.
        .a_wdata_i(AD), // Connect a_wdata_i to the fixed-cell AD pin.
        .a_wmask_i(AWM), // Connect a_wmask_i to the fixed-cell AWM pin.
        .a_rdata_o(AQ), // Connect a_rdata_o to the fixed-cell AQ pin.
        .b_cs_i(BCS), // Connect b_cs_i to the fixed-cell BCS pin.
        .b_we_i(BWE), // Connect b_we_i to the fixed-cell BWE pin.
        .b_addr_i(BA), // Connect b_addr_i to the fixed-cell BA pin.
        .b_wdata_i(BD), // Connect b_wdata_i to the fixed-cell BD pin.
        .b_wmask_i(BWM), // Connect b_wmask_i to the fixed-cell BWM pin.
        .b_rdata_o(BQ) // Connect b_rdata_o to the fixed-cell BQ pin.
    ); // End the portable SRAM behavior instance.
endmodule // End the KD28_SRAM_TDP_1024X128 functional cell.

`default_nettype wire // Restore implicit-net behavior after generated functional cells.
