# Actual fixed SRAM storage mapping proof

Execute on the confirmed local ASIC toolchain and authorized KD28 dependency. Per the user’s updated instruction, the root owns formal generation and production binding; separate agents own behavioral simulation, independent formal auditing, and TL/DL integration. Keep file ownership disjoint and review all evidence before consolidation. Keep production RTL and external dependencies unchanged. The preceding FIFO payload proof is complete for its logical synchronous SRAM contract; this stage proves the actual fixed-macro address, word-lane and registered-read mapping needed to use that contract in the production storage wrapper.

- [x] Elaborate the actual `kd28_fifo_sdp_storage_map` with its authorized fixed SRAM black-box declarations, common clock and explicit logical width/depth/address width. Preserve the complete graph. Extract each actual macro instance, check its fixed geometry and retain every nonmacro cell/state.
- [x] At the documented macro boundary only, replace each macro Q output by an arbitrary symbolic read word and expose all original macro input pins. Independently reconstruct the original graph to reject missing macros, altered state/clocks, hidden internal cuts or aliased lanes. This is a macro-interface proof; Q behavior remains the authorized SRAM model contract.
- [x] Prove all macro write/read enables, addresses, padded data, full byte masks and clocks against independent division/modulo geometry. Independently remember the bank of the last issued legal read and prove the entire output word selects that bank until another read; no output promise before the first legal read. Invalid addresses are not assumed away: pin mapping is checked for all inputs, while usable read data requires a legal most recent read. FIFO address bounds were separately proved.
- [x] Detect actual isolated source faults in write data/masks and registered bank selection. Distinguish reachable SAT counterexamples from elaboration or graph-audit failures. Exercise single-bank width tiling and multi-bank read selection.
- [ ] Close the remaining maximum-capacity formal query and expand parameter coverage across production widths/depths, partial final width lanes, macro-class boundaries and non-power-of-two/banked depths. Run actual behavioral macro simulation against an independent logical memory scoreboard; preserve the first-read, stalls, same-address read-before-write and bank-switch cases.
- [x] Independently audit complete pin/word coverage, source hashes, exact assumptions/commands and base/induction logs. Record precise passed configurations and unresolved composition/physical obligations before moving to the endpoint transaction path.

Planned entry: `python3 verification/sram_storage_map/run_formal.py --kd28-root PATH --label NEW --widths ... --depths ...`. Outputs are immutable actual-source snapshots, original/cut graphs, macro inventories, independent properties, SAT logs and results under `build/verification/sram_storage_map/NEW`. Next combine the mapping theorem with the FIFO payload invariant and actual storage wrapper wiring. Characterized macro timing, layout, full TL protocol and the final dual-IP Goal remain separate requirements.

Execution checkpoint: 95/96 formal configurations passed and independently audited. The 600-bit, depth-65535 case remains unproved after bounded SAT and bit-lowering attempts; this is a recorded residual, not a completed parameter proof. Actual behavioral simulation passed all 96 production parameter configurations, with 2 real faults detected. Production wrapper bindings passed 24 small-depth configurations. Full TL payload composition and characterized macro signoff remain open. Subsequent priority is `endpoint_transaction_plan.md`.
