# Memory RTL

Contains DMA, SRAM control, abstract HBM transaction logic, ECC, address maps, and QoS. External memory is
represented through the digital contract in `specs/interfaces/` and corresponding behavioral models.
