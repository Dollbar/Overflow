# Host Interface RTL

Contains command queues, completion queues, interrupts, address translation hooks, and host-facing digital
transaction interfaces. Protocol semantics are defined in `specs/abi/` and `specs/interfaces/`.
