# Continuous Integration

Contains formatting, schema validation, compiler and simulator tests, RTL lint and simulation, formal checks,
coverage, license checks, secret scans, link checks, and release audits. CI commands must be reproducible from
committed sources and must not depend on undeclared local state.
