# Deployment

Contains system images, containers, device plugins, cluster topology, model-shard deployment, health checks,
monitoring, upgrade, and rollback configuration. Production credentials, weights, and site-private settings
are never committed.
