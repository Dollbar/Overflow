# Drivers

Contains the Linux accelerator kernel driver, IOMMU/dma-buf support, command queues, interrupts, telemetry,
and user-space library. Kernel code may require a different license and must use explicit SPDX identifiers.

Drivers may not bypass version negotiation to access undocumented firmware or RTL registers.
