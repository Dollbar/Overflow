These are frozen pre-conversion typed TX leaf implementations using explicit XOR parity. They are independent golden references for combinational equivalence against the installed shared-primitive leaves. The runner copies and renames their module declarations only inside each fresh evidence directory. They are verification-only sources and must not be added to production RTL file lists.
