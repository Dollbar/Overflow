# Reuse transmit qualification before class selection

Execute alone with writing-plans, TDD, executing-plans and the established RTL quality workflow. Baseline: `1f917a1381277a080a700849d638d39b766c665a`, whose actual production mapping has complete post-reset correspondence. The actual worst timing path passes from header-cache validity through class qualification, selected-header requalification and packing to FIFO prefetch accounting.

Architecture: factor only the existing stateful source selection and Flit assembly into `rtl/tl/tl_tx_packer_core.v`. Its qualification inputs are `i_header_ready`, `i_nop_ready` and `i_has_data`. Keep `tl_tx_packer` as the unchanged public wrapper that computes those inputs from its original interface. In `tl_tx_channels`, route the already computed selected-lane qualification bits directly to the core, retaining raw selected payloads and existing class/Data ownership registers. No new state, latency, credit assumption or stall contract is introduced.

- [x] Add an elaborated-graph regression that the packing core's control outputs and next-state cones do not depend on raw Header/Tag/Data payload bits when qualification inputs are fixed. Observe the absent-core failure before implementation.
- [x] Extract the exact old selection/packing/register logic, preserve all four core state bits and all eight integrated channel state bits, and connect the prequalified channels. Update explicit source lists and meaningful mutation anchors so every supported runner still compiles the complete hierarchy.
- [x] Prove complete public-output and actual next-state equivalence of both standalone packer and integrated channels against the frozen baseline at WIDTH 8/16, then cover WIDTH 8..16 where parameter-dependent logic applies. Verify reset establishment and qualify actual RTL wiring mutations.
- [x] Run independent packer/channel vectors, production prepared-transmit unit and real SRAM peer/fault regressions, lint, static skill artifact checks and actual structural inventory. Preserve all failed and successful evidence.
- [x] Map the actual updated production top and measure its full physical matrix at the original IO/clock budgets and 64 fixed SRAM interfaces. Audit the new actual mapped correspondence before adopting the physical result. Record remaining timing gaps and full dual-IP obligations.

New core ports keep the original clock, synchronous reset, taken, pending, Auth and source payload interfaces; only the decoded qualification inputs are internal. `ready` includes position/payload/catch/credit checks. `nop_ready` is gated by candidate, credit and failed catch budget, with the core retaining the original pending-zero restriction. `has_data` remains the selected raw tenure result even when a held selection bypasses current readiness. This distinction is required for unconditional same-state equivalence, including inconsistent external source changes during stalls.

Commands and artifacts are documented in the new verification entries under `verification/tl_tx_qualification`. Outputs use fresh labels under `build/verification/tl_tx_qualification`; actual full-top mapping stays under `build/verification/tl_tx_prepared`. Next audit physical improvement, then continue the original full payload/Endpoint/Switch/PHY/INC/security/manageability Goal.
