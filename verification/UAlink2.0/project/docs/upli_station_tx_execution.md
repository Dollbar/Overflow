# Actual four-channel native TX aggregation

This increment follows upli_station_integration_execution.md and retains the complete Endpoint/Switch goal. It implements a reusable aggregate of both roles' transmit services, not a physical single-side station or complete station RX/RAS. Originator Request/OrigData use one shared sender; Completer Read Response and Write Response each use independent senders and phases. Four credit banks remain owned by those three senders.

The aggregate exposes distinct Originator and Completer credit-connected and beats-connected inputs, one common clock/reset, full native typed outputs, independent candidates and four returned-credit buses. It does not manufacture peer connection acknowledgement. Tests must provide two actual connection_side instances with external crossing and actual receive_channel storage/returns. Four credit guards check received parity; diagnostics are exposed and never converted into a claim of recovery. In this normal-path increment guard diagnostics do not invent a credit-drop/isolation policy. Reset disables checking; an enabled guard checks idle valid parity too.

Root owns build/development/upli_station_tx and this document. Sender and credit-adapter agents own their separate candidate directories; no common production RTL changes before candidates freeze. First record the absent-module/interface red, then add the aggregate and test actual three-sender/four-bank/four-receiver traffic with independent external queues and phase/credit observations. Parameters cover 1/2/4 ports and multiple capacities. Compare complete accepted payloads to real native events and then actual SRAM retirement, with candidate noise after acceptance, independent initial phase, credit exhaustion/return, reset and real wiring/parity faults. Never build expected payloads from DUT tail storage or credit state.

Implemented deliverables: rtl/upli/upli_station_tx.v, portable verification/upli_channels runner/TB and exact source/log/hash evidence under build/verification/upli_channels/station_tx/LABEL. Reproduction command: python3 verification/upli_channels/run_station_tx.py --label NEW --kd28-root /path/to/authorized/Overflow. The final command and actual five-configuration results are recorded in upli_station_tx_integration_review.md. Next connect RX validation, response semantics, native Endpoint adapters and RAS ownership before replacing upli_station_port.
