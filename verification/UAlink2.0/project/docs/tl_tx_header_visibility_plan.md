# Separate header validity from qualification data

Continue the full dual-IP Goal alone, from the proven qualification-reuse RTL at `6839cac` after closing the selected-ready experiment. The selected-ready Boolean bypass did not remove the critical feedback path. Its current mapped WIDTH 8/16 measurements are worse than the proven baseline; preserve the completed experiment and restore the baseline before implementing this next candidate.

The actual baseline WIDTH 8 worst path starts at Header FIFO `cnt_cached[1]` and reaches the opposite Header FIFO `cnt_unread[1]` through header consumption and prefetch. In the physical netlist, path cell `_060149_` is an `OAI211D0BWP40P140` combining both cache-count bits with Header FIFO `reg_head[119]`. The RTL currently zeroes the entire cached header/tag word when invalid, before the channel decoders inspect that word. This places cache validity ahead of substantial qualification logic. The retained path/alias evidence is under `build/verification/tl_tx_qualification/ready_baseline_path_aliases.json` and `ready_header_gate_evidence.json`; the physical source is `qualification_reuse_physical/w8`.

Investigate an explicit internal visibility profile: keep ordinary FIFO/storage outputs zero when invalid by default, but permit the two transmit Header FIFOs to supply their retained raw word to qualification logic. The channel must still gate candidate eligibility with the original valid flag and preserve the original zeroed payload, tags and has-data behavior when assembling output. Existing public channel callers keep their current behavior by default. Do not add state or a cycle, expose stale data on current public outputs, alter capacity/consumption, or change Data-bank behavior.

- [x] Prove the composite raw-header/valid qualification identity and qualify a real negative that omits the required invalid payload/tag masking, including held and arbitrary state cases.
- [x] Add narrowly scoped, validated parameters with original defaults to FIFO/storage and channel logic; enable the raw profile only for the two transmit Header FIFOs. Keep every existing public port and standard interface intact.
- [x] Prove complete actual state and output correspondence of the updated composition, and unchanged default FIFO/channel semantics. Cover WIDTH 8–16, queue depths and invalid parameter handling where applicable.
- [x] Run independent FIFO, channel and production prepared-transmit unit/peer models, real faults, ownership conservation, lint and source/artifact checks.
- [x] Map the actual updated production top at the unchanged libraries, 64 macro interfaces and original clock/IO budgets. Measure and audit all 60 profiles, prove current mapped correspondence, and adopt only on supported physical and semantic evidence.
- [ ] Record the new critical path, any remaining setup/hold gap, and continue the full Endpoint/Switch, protocol/payload, digital PHY, INC, security and management work.

The narrow parameterized change is adopted at source checkpoint `1194c73`. Final evidence includes both nine-width channel comparisons, 36 FIFO and 27 buffered composition configurations, 16 profile checks, 32 actual peer configurations, 27 ownership configurations, 12 actual mapped partitions and all 60 physical measurements. The final normal/optimized mapped, physical, ownership and wire audits agree. Setup improves by 47.155/27.000 ps at WIDTH 8/16; main 640 ps timing still closes 0/30 profiles and worst hold remains −8.036 ps. The new header-data-to-opposite-FIFO-count critical paths are recorded in the review. The final checklist item remains open because it includes the full dual-IP Goal.
