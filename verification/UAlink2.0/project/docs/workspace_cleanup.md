# 工作区产物清理记录

2026-09-11 按用户“清理过时文件释放空间”的授权，对 UALink 工程进行了有界清理。操作基于源码提交 `7e8896ad624a4b74474ab8843f4cee9780c6db6d`；没有修改 RTL、规范、库存或其他仓库，没有提交本记录。

本轮清单标识为 `workspace_20260911_141119`，详细证据保存在工程本地 `build/maintenance/workspace_20260911_141119/`。该目录属于不随源码发布的本地维护证据；本页记录结果，不宣称发布仓库包含这些产物。

## 范围与保留策略

扫描 `build/`、`reports/` 和存在时的 `artifacts/`，不跟随目录符号链接。`build/publication/overflow/` 整个活跃独立 Git 克隆及其 `.git` 完全排除；清理期间其他代理在该克隆运行回归和发布，不受本轮操作影响。私有规范、外部库和其他工程均不在删除范围。

删除普通产物必须同时满足：未被 Git 跟踪、至少两小时前生成、大小不少于 1 MiB、明确属于可重建格式、文件头确认生成类型，且扫描到删除期间 inode/大小/mtime 不变。实际符合条件的格式为 Icarus `.vvp` 和 Yosys `.blif`、`.il`；本轮没有删除满足该筛选条件的波形文件。删除前检查了工程活跃进程、工作目录及打开的文件，未发现源工程回归占用候选文件。其他工程正在运行的 xsim 未被操作。

以下内容全部保留：

- 路径任一部分包含 `capacity` 或以 `reset_final` 开头的本轮回归文件，不受后缀和大小影响。
- `snapshot`、`snapshots`、`source`、`sources`、`source_snapshot` 目录中的源快照。
- 历史通过/失败日志、结果、summary、manifest、脚本、普通源码及 JSON 图证据；其中部分 JSON 是大体积形式图，因后续审核可能需要，保守保留。
- 未确认可重建的格式、Git 跟踪文件、打开或正在修改的文件，以及整个 Overflow 克隆。

唯一整目录例外是已获单独授权的 `build/publication/pre_partition_snapshot/`。该目录是旧发布布局的重复备份；负责人此前已逐文件核对并确认新布局推送至 Overflow 提交 `5d5916b`。本轮删除该备份的全部文件和随后变空的目录，不将其误当仍需保留的独立源快照。

## 实际释放量

扫描清理范围内共 105,995 个普通文件，逻辑大小 90,194,628,338 bytes，不含活跃 Overflow 克隆和维护记录目录。扫描时最大的两个子树分别是 `build/verification/tl_tx_prepared` 的 34,676,704,960 bytes 和 `build/verification/tl_tx_qualification` 的 26,844,501,416 bytes。

| 删除类别 | 逻辑字节数 |
|---|---:|
| 旧 `.vvp` 仿真可执行产物 | 8,156,597,921 |
| 可重建 `.blif` 网表 | 10,765,660,579 |
| 可重建 `.il` 网表 | 2,201,569,516 |
| 已确认过时的发布备份 | 121,989,971 |
| 合计：3,805 个文件 | **21,245,817,987** |

删除文件记录的已分配块大小合计为 21,254,320,128 bytes。实际磁盘可用空间从 269,252,145,152 增至 **290,505,711,616 bytes**，观测增加 21,253,566,464 bytes；与文件块计数略有差异，因为其他进程仍在写入文件。这里分别记录逻辑大小、分配块大小和磁盘实测值，避免把它们混为同一个释放数字。

删除后逐路径检查了剩余 102,190 个已扫描文件，意外缺失为零；备份目录已移除，Overflow `.git` 仍存在。清理没有重跑协议、形式或时序测试，不产生新的功能通过声明。

## 本地证据及历史哈希边界

维护目录包含：

- `inventory.json`：初始目录/后缀空间统计、文件大小/mtime/inode及进程检查记录。
- `deletion_plan.json`：精确候选路径、大小、分配块、删除理由与保护策略。
- `deletions.jsonl`：实际逐文件删除路径、字节数、原 mtime/inode 和删除时间。
- `summary.json`：实际删除计数、磁盘前后值、保留文件检查及 Overflow 保护结果。

可以从工程根运行 `python3 -m json.tool build/maintenance/workspace_20260911_141119/summary.json` 查看本轮摘要。检查删除项应读取清单，不应递归跟随发布兼容布局中的符号链接。

**历史结果中引用已删除 `.vvp`、`.blif`、`.il` 或旧发布备份的 artifact hash，现在是原验证发生时的历史记录，并不代表所有对应文件仍可在本机逐项打开或重新哈希。** 日志、结果和源快照没有因此变成新的测试结果；需要重审被删产物时，应使用保留的脚本、源快照、参数和工具版本重新生成，再明确记录新的证据。尚存的 JSON 图及原始哈希未被改写为清理后的“全文件仍在”声明。

后续清理继续采用显式路径清单和活跃进程排除。本轮保留的大型 JSON 图、文本证据和未确认目录不自动成为下一轮删除授权；不得用整目录删除 `build` 或 `reports` 的方式替代逐项判断。
